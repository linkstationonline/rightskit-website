---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<span
  class:list={[
    "inline-flex items-center bg-surface px-3 py-1.5 font-plex text-[0.6875rem] font-light tracking-widest text-brand uppercase",
    className,
  ]}
>
  <slot />
</span>
