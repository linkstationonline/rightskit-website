---
import Tag from "./Tag.astro";

interface Props {
  title: string;
  body: string;
  tags: string[];
  /** inline Lucide SVG markup for the icon above the heading */
  icon?: string;
  class?: string;
  /** stagger index for the sequential scroll reveal */
  index?: number;
}
const { title, body, tags, icon, class: className, index = 0 } = Astro.props;
---

<div class:list={["reveal", className]} style={`--reveal-delay:${index * 130}ms`}>
  <div
    class="border-r-2 border-r-transparent px-[max(22px,5%)] pt-14 pb-14 transition-colors duration-200 hover:border-r-brand hover:bg-ink/[0.035] sm:px-8 lg:pt-16 lg:pr-10 lg:pb-16 lg:pl-12"
  >
    {
      icon && (
        <span
          class="mb-4 block text-ink-muted [&>svg]:h-5 [&>svg]:w-5"
          aria-hidden="true"
          set:html={icon}
        />
      )
    }
    <h3 class="text-[23px] font-[470] leading-[1.15] text-ink">{title}</h3>
    <p class="mt-3 max-w-[530px] text-[16px] leading-[25px] font-[380] text-pretty text-ink-muted">{body}</p>
    <div class="mt-8 flex flex-wrap justify-start gap-2 sm:justify-end">
      {tags.map((t) => <Tag>{t}</Tag>)}
    </div>
  </div>
</div>
